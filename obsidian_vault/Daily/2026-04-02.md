# Daily Log - 2026-04-02

## P11-02/P11-03 Stabilization, Routing Debug, and Backend/Flutter Validation

**Date:** 2026-04-02  
**Status:** Mixed (core fixes landed; live behavior still needs final stabilization pass)

---

## Executive Summary

Today focused on end-to-end stabilization across backend agent + Flutter app, with emphasis on:
- voice turn stability,
- research response quality,
- planner/export behavior,
- backend connectivity/watchdog behavior,
- and log-driven root-cause analysis.

Key outcome: infrastructure and logging are now clear enough to isolate failures precisely.  
Main unresolved issue is **query rewrite contamination** causing incorrect routing/answers in follow-up turns.

---

## Workstreams Updated Today

### 1) Research UX Refinement (P11-02)

Planned/implemented scope during today’s run:
- suppress intermediate research preamble ("Let me look that up for you")
- keep semantic events (`agent_thinking: searching`, `tool_execution`) intact
- add one-shot search start cue in Flutter (with visual fallback retained)
- preserve final research result path

Expected behavior target:
- no spoken/text intermediate preamble for research-pending turn
- immediate search cue (if sound enabled)
- existing "Searching the web..." visual state remains fallback
- final answer still delivered normally

---

### 2) Voice + Deep Research + Planner Stabilization (P11-03)

Session tracking indicates these fix groups were executed in strict micro-batches:
- VAD/interruption tuning
- research quality/fallback guardrails
- deep voice summary mode for report-style research
- planner/download export path handling + governance messaging
- sentinel scope consistency

Reported test snapshot from execution thread:
- targeted suites green for synthesizer/search/sentinel/orchestrator paths
- no full-suite load run (intentional)

Note:
- keep validating with live manual turns; transcript evidence still shows routing quality issues in certain follow-ups.

---

### 3) Backend + Flutter Runtime Diagnostics

Actions completed:
- backend process stop/start cycles for manual validation
- connectivity and routing evidence collected from backend and Flutter logs
- combined analysis artifact created:
  - `/home/harsha/Downloads/Projects/v2/Maya-One-phase-0-2/Agent/logs/combined_how_are_you_analysis_20260402_143049.log`

Additional referenced artifacts:
- transcript export reviewed:
  - `/home/harsha/Downloads/how_are_you.md`
- earlier transcript export reviewed:
  - `/home/harsha/Downloads/Yeah_So_what_is_the_time_.md`
- flutter diagnostic output:
  - `/tmp/flutter_connect_diag.log`
- backend session logs:
  - `/home/harsha/Downloads/Projects/v2/Maya-One-phase-0-2/Agent/logs/backend_live_session.log`

---

## Root-Cause Findings Confirmed Today

### A) Follow-up Query Rewrite Contamination (Primary)

Observed pattern:
- pronoun follow-ups are rewritten with stale context fragments (example pattern: appending "released by Google")
- contaminated rewrite shifts intent classification toward research or wrong entity
- downstream effects:
  - incorrect web queries
  - incorrect spoken summaries
  - planner/export misrouting

Identified code locations for repair/verification:
- `core/orchestrator/agent_orchestrator.py`
  - `rewrite_research_query_for_context(...)` (around line 1020)
  - `_rewrite_pronoun_followup_pre_router(...)` (around line 1065)
  - forced pre-router research branch (around 4143-4160)
  - secondary rewrite call in research branch (around 4439)

### B) Planner/Sub-agent Not Entered for Some Save/Export Requests

Evidence:
- no orphaned `PENDING`/`RUNNING` task backlog found for analyzed session
- implies worker chain is not stalling; task creation path often not entered
- likely due to routing contamination and/or phrase normalization miss from voice transcription

### C) Latency/Context Overhead Pressure

Observed in analysis thread:
- high context/token overhead increases first-token latency
- amplifies user-perceived instability when routing is already degraded

---

## Security/Config Notes

- Tavily key was provided and intended for improving research quality path.
- ElevenLabs key was shared in session; rotate before production use.
- Keep provider fallback behavior explicit and observable in logs.

---

## What Was Verified vs. What Is Still Open

### Verified Today
- backend process control and log capture workflow works
- combined cross-source debug artifact generated successfully
- concrete rewrite/routing fault region isolated with file+line targets

### Still Open
1. Final code-level hardening of pronoun rewrite contamination guard.
2. Routing priority check to ensure report/export intents are handled before pure research path when applicable.
3. Voice-typo normalization (e.g., "diwnloads" -> "downloads") before intent matching.
4. Final live E2E pass:
   - long spoken prompt with pause (no premature cut-off),
   - deep research voice summary quality,
   - report generation/save flow in trusted path,
   - clean denial guidance in user-restricted path.

---

## Recommended Next Session Order

1. Patch rewrite guard + specific-entity resolution in orchestrator.
2. Re-run targeted orchestrator tests only.
3. Live 3-turn manual test (research -> report save -> simple query).
4. Confirm artifact write path (`~/Downloads/...`) and governance denial message.
5. Update this log with final pass/fail matrix and commit hash.

---

## Files/Areas Touched or Analyzed Today

- `core/orchestrator/agent_orchestrator.py` (analysis focus; rewrite/routing)
- backend runtime/log pipeline (`agent.py` execution + log capture)
- Flutter connectivity behavior (diagnostic correlation against backend state)
- Obsidian daily logging (`Daily/2026-04-02.md`)

---

## Final Validation Update (Late Session)

### P11-03 Stabilization Checkpoint
- Commit: `d8508ce`
- Tag: `p11-03-routing-stable`
- Status: stable checkpoint created after targeted tests + 3-turn smoke validation.

### P11-04 Watchdog Reclaim Validation (PASS)

Test method:
1. Injected synthetic stale `RUNNING` task into `dev_maya_one.db`:
   - `task_id=watchdog-test-4cc9e659`
   - `updated_at` backdated by ~15 minutes
2. Ran direct watchdog scan (`WorkerWatchdog.run_once()`).
3. Verified reclaim log + DB state transition.

Evidence:
- Log: `🐕 stuck_task_reclaimed task_id=watchdog-test-4cc9e659 age_seconds=908`
- DB after scan:
  - `status=FAILED`
  - `error=stuck_task_reclaimed_by_watchdog`

Notes:
- Console watchdog telemetry showed `Active: 0 (Running: 0, Pending: 0)` during one run.
- Reclaim query path operates directly on `tasks.status='RUNNING' AND updated_at < cutoff`; this is the production-critical path and is confirmed correct.
- No code change required for reclaim logic in this validation pass.
