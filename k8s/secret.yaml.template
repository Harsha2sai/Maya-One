# Copy to secret.yaml, fill values, never commit the filled version.
# Apply with: kubectl apply -f k8s/secret.yaml -n maya
apiVersion: v1
kind: Secret
metadata:
  name: maya-secrets
  namespace: maya
type: Opaque
stringData:
  groq-api-key: "REPLACE_ME"
  livekit-url: "REPLACE_ME"
  livekit-api-key: "REPLACE_ME"
  livekit-api-secret: "REPLACE_ME"
  elevenlabs-api-key: "REPLACE_ME"
  deepgram-api-key: "REPLACE_ME"

