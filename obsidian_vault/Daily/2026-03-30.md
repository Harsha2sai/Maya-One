# Daily Log - 2026-03-30

## Phase 10 Closure Corrections + Phase 11 Start

**Date:** 2026-03-30  
**Status:** In Progress (Phase 11 active)

## What Was Done Today

### 1) Baseline + Release Corrections
- Corrected Phase 10 baseline validation from partial targeted run to batched baseline checks.
- Confirmed batched test totals: **186 passed** (62 + 26 + 28 + 70), 0 errors in those batches.
- Deleted incorrect `v0.10.0` tag and recreated it on validated commit.

### 2) Code Fixes Completed
- Fixed duplicate `coerce_priority` validator in Task model.
- Restored note tooling compatibility with SQLite helpers (`_get_db_path`, note CRUD support used by tests).
- Enforced SQLite task-store pragmas for WAL/NORMAL behavior.
- Added Phase 11 routing/parser fixes:
  - SmartLLM success-path memory message merge.
  - Router note-intent guard to prevent identity misroute.
  - Direct note tool fast-paths in orchestrator.
  - Scheduling parser support for calendar format: `for/on <time> called/titled <title>`.

### 3) Phase 11 Governance/Docs Updates
- Added Phase 11 scope to `stress_results/2026-03-phase9/SESSION_GAPS.md` (P11-01 to P11-04).
- Marked **P11-01 resolved** after runtime verification (`provider_supervisor_configured` observed).
- Set **P11-02 (Chroma / vector path issue)** as next active priority.

## Commits Made Today (Agent repo)
- `b7a1efb` — Fix duplicate coerce_priority validator in Task model
- `7b57744` — Fix note tools SQLite compatibility and enforce task store WAL pragmas
- `5cb99a4` — Document Phase 11 scope: P11-01 through P11-04
- `b6af42f` — Mark P11-01 resolved; set P11-02 as next Phase 11 priority
- `cb5bb6a` — Fix memory recall, note routing, calendar parser

## Validation Summary

### Batch validation (post-fix)
- Batch 1: `62 passed`
- Batch 2: `26 passed`
- Batch 3: `28 passed`
- Batch 4: `70 passed`

### Live checks (console)
- Provider supervisor: configured/active observed in logs.
- Research/scheduling handoff balance: request/completion events observed.
- Notes: create/read path now routes correctly in live checks.
- Calendar phrase (`for monday at 9am called kickoff`): now parsed/handled.

## Tags
- `v0.10.0` -> `7b57744` (validated)
- `v0.11.0` -> not tagged yet

## Current Open Issue
- Memory recall remains unstable/flaky in live runs.
- Observed related runtime failures include vector/operator errors and intermittent LLM stream timeout behavior.
- Next work remains P11-02: stabilize retrieval path and memory observability end-to-end.

## Next Steps
1. Add permanent per-turn memory pipeline audit logging in SmartLLM before API call.
2. Harden hybrid retriever error visibility/fallback behavior for vector query failures.
3. Re-run two-turn memory write/recall validation with audit logs enabled.

## Late Update (Memory Pipeline Audit + Permanent Write Hook)

### New Findings
- Confirmed `store_conversation_turn` was effectively only called in planner flow (`agent_orchestrator.py:3378`), not guaranteed in normal chat path.
- Confirmed SmartLLM had no per-turn memory injection audit line before this update.
- Confirmed retrieval can return memory (`memory_retrieve_results count=4`) while LLM payload still reports `smart_llm_context_audit memory_msgs=0 total_msgs=1`.
- Confirmed `[Memory]` tag was never produced by context pipeline; it only existed as a SmartLLM filter condition.

### Fixes Applied
- Added best-effort chat-memory write hook for queued chat responses in orchestrator:
  - `_store_chat_turn_memory(...)` added and invoked in `_handle_chat_response(...)` queue dispatch loop.
- Added permanent SmartLLM audit log before API context handoff:
  - `smart_llm_context_audit memory_msgs=<n> total_msgs=<n>`.

### Commit Added
- `cfc6025` — Permanent fix: memory write in chat path + audit log

### Validation After Fix
- Write turn now logs stored memory successfully:
  - `Stored conversation memory: ...`
- Recall turn still shows retrieval/injection mismatch:
  - `memory_retrieve_results count=4 ...`
  - `smart_llm_context_audit memory_msgs=0 total_msgs=1`
- Targeted backend suite still clean:
  - `62 passed` (`test_agent_orchestrator`, `test_handoff_manager`, `test_scheduling_agent_handler`, `test_calendar_tools`, `test_note_tools`, `test_task_store_sqlite`).

### Updated Open Issue Status
- Memory write-path gap is closed.
- Remaining issue is between retrieval output and final LLM context assembly/injection (memory reaches retriever logs but not final message list).
- This keeps P11-02/P11 memory stability work active.

## Latest Update (Memory Pipeline Deep Dive)

### Additional Commits Made
- `646e735` — Fix memory recall: remove dead `[Memory]` merge block, increase retrieval `k=4`
- `74c3012` — Fix memory `user_id` tagging: store and retrieve with consistent `user_id` key

### What Was Confirmed
- `VectorStore` is persistent (not ephemeral):
  - `ephemeral_fallback: False`
  - `persist_directory: /home/harsha/.maya/memory/chroma`
  - collection item count observed: `3555`
- Retrieval path works in isolation:
  - `HybridMemoryManager.get_user_context("runtime_user", k=4)` returns memory text
  - `retrieve_relevant_memories(..., user_id="runtime_user")` returns expected results
- New writes now include `user_id` metadata:
  - Probe after live run showed `Items with user_id=runtime_user: 2`
  - Included both turn-1 (`my name is Harsha`) and turn-2 conversation items

### Key Root Cause Found + Fixed
- Prior stored memories were missing `user_id` metadata, so `user_id`-scoped retrieval returned empty.
- Fixed by:
  - Extending `HybridMemoryManager.store_conversation_turn(...)` to accept `user_id`
  - Writing `user_id` into `MemoryItem.metadata`
  - Passing `user_id` from orchestrator chat write hook (`_store_chat_turn_memory`)

### Current Runtime Status
- Live two-turn console smoke test still fails behaviorally:
  - Turn 1 stores memory successfully
  - Turn 2 still answers as if no retained identity
- Logs during failing turn continue to show mismatch:
  - retrieval side reports non-zero memory results (`memory_retrieve_results count=4`)
  - `smart_llm_context_audit memory_msgs=0 total_msgs=1`

### Current Interpretation
- Storage + retrieval are now functioning for `runtime_user`.
- Remaining failure is in final context handoff/injection before LLM call (memory not surviving into final message payload).

### Next Immediate Work
1. Add explicit context-builder logging right after memory append to `system_content`.
2. Add handoff logging between context builder output and SmartLLM input (message roles/counts).
3. Verify exact return-path timing/order for chat write vs next-turn read under live loop.

## Final Update (Late Night Memory Debug Closure)

### Additional Commits (after prior daily entry)
- `f526408` — Fix memory user_id mismatch: write uses `queued_user_id` not `runtime_user` fallback
- `94361fa` — Fix memory timing: `await` write before resolving queue future so next turn can read latest turn

### What Was Validated in Live Runtime
- Captured full 3-turn console trace to `/tmp/maya_three_turn.log` (460 lines).
- Turn sequence used:
  1. `my name is Harsha`
  2. `what is 2 plus 2`
  3. `what do you know about me`
- Retrieval on turn 3 showed:
  - session scoped query (`session_id=console-room`) -> `count=0`
  - scope fallback (`session_id=none`) -> `count=4`
- Writes are now serialized before the next turn starts:
  - turn 1 write completed before turn 2 processing
  - turn 2 write completed before turn 3 processing

### Critical Observations
- `context_guard` reports non-zero memory token budget usage (`context_guard_tokens_memory=565`) in turn 3.
- `smart_llm_context_audit` still reports `memory_msgs=0 total_msgs=1`.
- This indicates retrieved memory is likely embedded inside the single system prompt block rather than represented as separate memory messages.
- Current audit counter is therefore not a reliable indicator of whether memory text exists inside system content.

### Current True Status
- ✅ Write path fixed and awaited.
- ✅ user_id write/read key alignment fixed (`console_user` path).
- ✅ Retrieval pipeline returns memories after fallback.
- ❌ End-user answer quality still wrong for identity recall (“I don’t know anything about you”).
- ❌ Session-scoped retrieval still empty because conversation memories are not stored with `session_id` metadata.

### Next Concrete Fixes
1. Store `session_id` in `store_conversation_turn` metadata and pass `queued_session_id` at write callsite.
2. Add context-builder log proving exact injected memory text snippet in final `system_content`.
3. Update SmartLLM audit metric to detect memory inside system prompt (not only separate message objects).
4. Add deterministic identity-memory routing for prompts like `what do you know about me` and `what is my name`.

### End-of-Day Summary
Today's work closed major structural gaps (write timing, user_id mismatch, queue ordering, persistence checks), but memory recall is still not production-correct because session-scoped recall and final prompt behavior remain unresolved.

## Post-Midnight Continuation (2026-03-31, added to 2026-03-30 report)

### Release/Test Contract Fixes Completed
- Fixed router fast-path contract drift test:
  - `tests/test_agent_router.py` now expects `{"time","app","media","youtube","notes"}`.
- Fixed LiveKit test warning (`LLMMetrics.request_id` must be string):
  - `tests/tools/test_livekit_injection.py` now yields a real `ChatChunk(id="test-id", ...)` instead of a bare `MagicMock` id.
- Commit created:
  - `ff69c08` — `tests: align fast-path contract with notes group; fix LiveKit metrics mock request_id`

### Tag Correction
- `v0.11.0` was moved to the clean post-fix commit:
  - `v0.11.0 -> ff69c08`

### Memory Follow-up Fixes Implemented (Post-Plan)
- `core/context/context_builder.py`
  - Legacy `__call__` memory retrieval `k` is now config-driven (`CHAT_RETRIEVER_K` / `CHAT_MEMORY_TOP_K_DEFAULT`) instead of hardcoded.
- `tests/performance/test_prompt_size.py`
  - `test_memory_retrieval_limited` updated to assert configured chat retrieval `k` (not fixed `k=1`).
- `tests/test_hybrid_memory_manager.py`
  - Added regression test to ensure conversation memory persists both `user_id` and `session_id` metadata.

### Validation (Chunked to Reduce System Load)
- Full `tests/` run executed in 10 small chunks (12 files/chunk, final chunk 3 files).
- Result: `ALL_CHUNKS_OK=true`.
- No background pytest processes left running after chunked validation.

### Runtime Memory Smoke (Current Truth)
- 3-turn console smoke:
  1. `my name is Harsha`
  2. `what is 2 plus 2`
  3. `what do you know about me`
- Observed:
  - `context_guard_tokens_memory > 0`
  - `smart_llm_context_audit memory_present=True`
  - Turn 3 answer contains correct recall: `Your name is Harsha.`
- Remaining quality issue:
  - Turn 1 can still show stale/poisoned historical memory influence (assistant wording contamination), so memory pipeline is structurally working but not yet behaviorally clean in all cases.

### Updated Status Snapshot
- ✅ Contract/test failures from this cycle are fixed.
- ✅ `v0.11.0` now points to validated post-fix commit.
- ✅ Memory write/read/recall path works in live smoke.
- ❌ Memory quality/isolation hardening still pending (historical contamination cleanup + stronger retrieval/write guards).
